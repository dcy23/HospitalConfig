server:
  port: 8092

# actuator
management:
  endpoints:
    web:
      exposure:
        include: '*'
        # include: refresh

process:
  enabled: false

process-config:
  enabled: true
  max-attempts: 3
  options: option1, option2